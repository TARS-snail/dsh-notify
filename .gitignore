# Dependencies
node_modules/

# npm cache redirected into the workspace (sandboxed environments)
.npm-cache/

# Build output (built on install via the `prepare` script)
lib/

# Research scratch files (docs downloaded while studying the spec)
.research/

# OS / editor noise
.DS_Store
*.log
